services:
  - type: web
    name: cms-backend
    runtime: node
    rootDir: cms
    buildCommand: npm install && npm run build
    startCommand: npm run start

  - type: web
    name: gallery-frontend
    runtime: node
    rootDir: gallery
    buildCommand: npm install && npm run build
    startCommand: npm run start
